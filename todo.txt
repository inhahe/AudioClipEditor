let's have save/load project if it doesn't already exist, and ask to save project before exiting if any changes haven't been saved

--

yes, let's add 'capture from whole clip' and also capturing from directly inside the editor
^ doing